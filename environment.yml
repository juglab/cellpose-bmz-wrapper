name: cellpose
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python=3.9
  - pytorch
  - torchvision
  - setuptools
  - bioimageio.core
  - imagecodecs
  - pip
  - pip:
      - matplotlib
      - tqdm
      - cellpose==3.1.0
